/*#include<iostream>
#include<math.h>
#include<iomanip>
using namespace std;

int main()
{
	double x;
	cin >> x;
	if (-1 <= x&&x <= 1)
	{
		double y = 0.0;
		y = acos(x);
		cout << setiosflags(ios::fixed);                 //这个与setprecision(2)联合才能实现。
		cout << setprecision(5) << y << endl;
	}
	
	system("pause");
	return 0;
}*/